import { View, Text } from 'react-native';

export default function SelectDays() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Select Days Screen - You can build this later</Text>
    </View>
  );
}
